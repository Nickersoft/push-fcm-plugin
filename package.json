{
  "name": "push-fcm-plugin",
  "version": "0.0.1",
  "description": "Firebase Cloud Messaging plugin for Push.js",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf bin",
    "build": "mkdir -p bin && uglifyjs index.js -o bin/index.min.js --source-map filename=bin/index.min.js.map --comments",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tyler Nickerson",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "uglify-js": "^3.0.16"
  }
}
